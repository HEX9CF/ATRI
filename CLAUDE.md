## Rules

- When you update a tool script , you must update the matching document under `docs/` with the same base name in the same change.
- Keep the script behavior and the document examples, defaults, and output descriptions consistent.
- If a tool has no matching document yet, create one before finishing the change.
